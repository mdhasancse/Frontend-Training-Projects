
// Converts an array to a string, representing the array elements separated by commas.

const colors = ['red', 'green', 'blue'];
const colorsAsString = colors.toString();
console.log(colorsAsString); // Outputs: "red,green,blue"
