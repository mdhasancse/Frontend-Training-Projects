

// The replace method searches the string for the first occurrence of the first argument passed and replaces it with the second argument. 
//If the searched argument is not present in the string, the function returns the string itself



// Syntax:
//string_variable.replace(‘first_value’, ‘second_value’);


var str = "Hello Cybrosys";
var replace_expr = str.replace('Hello', Hai);
console.log(replace_expr); // The console gives ‘Hai Cybrosys’, because the function replaced the first argument ‘Hello’ with ‘Hai’.
