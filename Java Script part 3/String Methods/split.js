
// The split method splits the string into an array of substrings, excluding the arguments passed to it, and returns the array. 
//We can also pass a limit to limit the elements in the array returned.


// Syntax:
// string_variable.split(‘character’, ‘limit’)

var str = "Hello Cybrosys xencia";
var split_expr = str.split(' ');
console.log(split_expr);
console.log(split_expr.length)-1;